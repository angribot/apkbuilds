# Publish orphan Pages snapshots

Serve the APK repository from an orphan `gh-pages` snapshot and replace that
snapshot on publication. This preserves the complete live repository without
accumulating binary history, at the cost of keeping no audit history on the
publication branch.
